// DESCRIPTION :  Defining a node in the grid
// More attributes to be added as and when required.
// Example, we can add weights for a particular node later.


function node(x, y, walkable) {

    // (number) x = x co-ordinate of node
    this.x = x;

    // (number) y = y co-ordinate of node
    this.y = y;

    // (boolean) walkable = whether this node can be walked through
    //CODE TO BE WRITTEN

}

module.exports = node;